import 'package:flutter/material.dart';

class AppPadding {
  static const allSmall = EdgeInsets.all(10);
  static const allMedium = EdgeInsets.all(20);
  static const allLarge = EdgeInsets.all(40);
  static const bottomSmall = EdgeInsets.only(bottom: 10);
  static const bottomMedium = EdgeInsets.only(bottom: 20);
  static const bottomLarge = EdgeInsets.only(bottom: 40);
  static const topLarge = EdgeInsets.only(top: 40);
  static const horizontalSmall = EdgeInsets.all(10);
  static const horizontalMedium = EdgeInsets.symmetric(horizontal: 20);
  static const horizontalLarge = EdgeInsets.symmetric(horizontal: 40);
  static const verticalLarge = EdgeInsets.symmetric(vertical: 40);
  static const verticalMedium = EdgeInsets.symmetric(vertical: 20);
  static const verticalSmall = EdgeInsets.symmetric(vertical: 10);
  static const rightSmall = EdgeInsets.only(right: 10);
  static const leftSmall = EdgeInsets.only(left: 10);
  static const rightLarge = EdgeInsets.only(right: 40);
}
